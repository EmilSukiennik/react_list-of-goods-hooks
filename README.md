A simple React-based web app that displays a list of goods and allows users to sort them by three different criteria with the click of a button. The interface is clean and responsive, using React state management to dynamically update the list based on the selected sorting method.

### PREVIEW:
https://emilsukiennik.github.io/react_list-of-goods-hooks/
